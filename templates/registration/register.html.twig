{% extends 'base.html.twig' %}

{% block title %}Création de compte{% endblock %}

{% block body %}

    <main>

        <div class="form-wrapper offset-lg-4 col-lg-4 offset-md-1 col-md-10 d-flex flex-column">

            {% for flashError in app.flashes('verify_email_error') %}
                <div class="alert alert-danger" role="alert">{{ flashError }}</div>
            {% endfor %}

            {% for message in app.flashes('success') %}
                <div class="alert alert-success">
                    {{ message }}
                </div>
            {% endfor %}

            <h1 class="d-flex justify-content-center">Création de compte :</h1>

            {{ form_start(registrationForm) }}
            {{ form_row(registrationForm.email, {
                label : 'Adresse e-mail :'
            }) }}
            {{ form_row(registrationForm.plainPassword, {
                label: 'Mot de passe :'
            }) }}
            {{ form_row(registrationForm.username, {
                label: 'Nom d\'utilisateur :'
            }) }}
            {{ form_row(registrationForm.firstName, {
                label: 'Prénom :'
            }) }}
            {{ form_row(registrationForm.lastName, {
                label: 'Nom de famille :'
            }) }}
            <button type="submit" class="btn btn-lg btn-primary">Créer un compte</button>
            {{ form_end(registrationForm) }}

        </div>

    </main>




{% endblock %}
